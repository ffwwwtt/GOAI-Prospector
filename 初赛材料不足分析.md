# 初赛提交材料不足分析

> 对照文档：`补充.md`（竞赛指南与评审标准）、`赛题内容.md`（赛题要求）
> 提交材料目录：`aaa/`

---

## 🔴 一、路线 A 核心验证标准未达成

**依据**：`补充.md` 路线 A 明确要求——「新生成的规律必须在统计指标上优于前人成果（例如更高的 R² 或更低的 MSE），且能解释为何旧模型会失效。」

**实际情况**：
- 材料中**多处自述"候选=基线，无提升"**：
  - MOF 缺陷/容量模型对比：「候选=基线，如实记录'无提升'」
  - 热电模型对比：「候选=基线（无提升）——如实记录」
- **Slack 经典模型对照未实际执行**：工具搭好了（自检 R²=0.994），但因「钙钛矿语料缺 (T, E_g) 配对数据」，实际对比结果是「数据不足」。这等于是**路线 A 最核心的"挑战旧模型→提出修正规律"闭环没有实证完成**，只有一个空工具。
- 这意味着路线 A（占 50% 分值）的**核心得分点——「优于前人」——在材料中无正面证据**，只有"诚实披露的负结果"。

> 诚实披露是好事，但如果全部主要结果都是"无提升"或"数据不足"，评委无法给路线 A 打出有效分。

---

## 🔴 二、LLM-搜索融合深度在主案例中不足

**依据**：`赛题内容.md` 要求「搜索方法需与 LLM 深度融合，而非仅用 LLM 生成搜索代码。考察 LLM 如何参与搜索过程——例如生成候选假设作为搜索种群的种子、在搜索中评估中间结果的科学合理性、引导搜索空间的剪枝与聚焦等。」

**实际情况**：
- 材料（01_初赛提交材料.md 第 199 行）明确写道：**「LLM 中间评估引导于跨主题验证阶段启用（钙钛矿 12 次、热电 4 次）；MOF 主案例的贝叶斯搜索（328 候选点）为其上线前运行」**
- 这意味着作为核心展示的 **MOF 主案例实际上没有启用 LLM 中间评估引导**，贝叶斯搜索是传统方式跑的。LLM 只在假设生成阶段参与，搜索过程中并未深度介入。
- 跨主题验证虽启用了 LLM 引导，但仅钙钛矿 12 次、热电 4 次——call 次数很少且不是主案例，说服力大打折扣。

---

## 🟡 三、运行轨迹证据链断裂（3 个验证主题中 2 个缺失）

**依据**：`补充.md` 红线一「每个结论都能指回具体文献或数据库记录，一层层可追」；Sciverse 调用记录天然是审计证据链。

**实际情况**：
- 跨主题验证报告（03_跨主题验证报告.md 第 57 行）明确承认：**「其余主题轨迹因 --fresh 清理机制未留存，但完整产出归档与证据链均在本包可核验，也可按复现说明重新生成」**
- 四个主题中，只有 battery 主题保留了完整运行轨迹；钙钛矿和热电主题**没有运行轨迹**——评审无法追溯这两个主题的检索/推理/决策过程。
- 虽然产出文件还在，但「证据链四层追溯（p# → papers.json → search_log → 轨迹）」的**最底层断了**。评委如果追问"你是怎么搜到这 96 篇钙钛矿论文的？"——拿不出 search_log 和轨迹。

---

## 🟡 四、Sciverse API 和 MinerU 实际接入状态存疑

**依据**：`赛题内容.md` 推荐使用 Sciverse API（其调用记录天然构成可审计证据链，评审加分项）和 MinerU（PDF 解析）。

**实际情况**：
- 两个核心推荐工具在材料中均标注为**「可选」**：
  - `SCIVERSE_API_KEY=sci_xxx（文献检索，可选）`
  - MinerU「缺失时自动回退本地 markitdown_utils」
- 如果实际运行中 Sciverse 和 MinerU 都没有真正接入，就会丢失：
  - Sciverse 的**审计证据链加分项**
  - MinerU 的高质量 PDF 解析能力
- 材料中 MOF 主案例来源显示 `Sciverse 169 + arXiv 11`，说明 Sciverse 是接入了的。但 MinerU 是否真正使用没有明确证据（报告中无 MinerU 解析产物的引用），PDF 解析质量可能全依赖 markitdown 回退方案。

---

## 🟡 五、模型统计对比结果缺乏正面支撑

**依据**：`补充.md` 路线 A 验证标准「新生成的规律必须在统计指标上优于前人成果」。

**实际情况**：
- MOF 缺陷/容量：n=37，**候选=基线**
- 热电 ZT：n=9，**候选=基线**
- 钙钛矿带隙：**n=1，「定量样本不足」**
- Slack 经典模型：**无数据，未执行**
- 所有模型对比都**没有出现候选优于基线的情况**——不是某个假设没通过，是**全部没有正面结果**。这可能让评委质疑：是方法本身无法产出优于基线的规律，还是选题/数据问题？

---

## 🟠 六、独立系统说明文档未包含在提交包内

**依据**：`补充.md` 第五节要求初赛提交「Agent 系统说明（架构清晰，有增量）」。

**实际情况**：
- 01_初赛提交材料.md 第五节确实有系统说明，但内容较概括（一张架构表格 + 四行版本列表），并在第 148 行写明：**「详细系统说明（架构分层、24 工具清单、预算管理、checkpoint、上下文压缩、跨轮记忆、证据链审计）见代码仓库 docs/system_description.md」**
- 如果提交包中没有包含 `docs/system_description.md`（提交清单里没有列出），评委看到的就只有主文档中的简要版本。**架构清晰度和增量说服力全靠这几段概括文字**，可能不够。

---

## 🟠 七、跨主题验证的参照系设计未执行

**依据**：跨主题验证报告（03_跨主题验证报告.md 第 51 行）提到「参照系：贝叶斯 vs 随机搜索同预算对比设计（排除'随机运气'）」

**实际情况**：材料中提到了这个设计（贝叶斯 vs 随机搜索对比），但**没有任何一处展示了实际的对比结果**。这个实验如果没做，就不应该列在通用性设计依据里——写出来了但没数据支撑，评委追问时会很被动。

---

## 🟠 八、初赛提交包的证据目录结构与说明不完全一致

**依据**：00_提交说明.md 列出 `evidence/` 目录下有知识图谱、Gap 报告、发现报告、新颖性核查、模型对比等。

**实际情况**：
- 00_提交说明.md 第 37 行提到：「运行证据/（zip 内）运行轨迹、跨轮记忆、Agent 自写脚本、四主题完整运行产出归档」
- 这些是打包在 zip 里的还是已经放在 aaa 目录下？目前 aaa 目录只有 4 个 md 文件。如果运行证据没有随附在提交包中（而是"zip 内另附"），需要确保打包时不会遗漏。

---

## 🟢 九、次要问题

| # | 问题 | 依据 |
|---|------|------|
| 9 | 许可证「MIT（待定）」——"待定"不正式，初赛虽不强制开源但会影响开源计划的清晰度 | `赛题内容.md` 5.1 |
| 10 | 补充.md 冲高分方向要求「跨领域发现：在不同子领域的文献之间建立连接」，四主题是独立运行的，没有跨子领域连接的展示 | `补充.md` 第八节 |
| 11 | MOF 主案例实际跑了 91 轮（首轮？+ 深化轮），但写的是"24 轮检索"——"91 轮"和"24 轮检索"的概念关系不够清晰（91 是 Agent ReAct 轮数，24 是检索工具调用次数？），评审可能困惑 | 01 材料 vs 02 报告 |
| 12 | 材料中"180篇唯一论文"的捕获效率 86%——分母是什么？86% 意味着有 14% 的检索结果被丢弃，丢弃标准未说明 | 02 报告 |

---

## 📊 总结：最需要关注的三个致命问题

1. **路线 A 的核心得分点缺失**：所有模型对比都是"无提升"+"数据不足"，没有一个正面验证了"新规律优于旧模型"——这占 50% 分数。
2. **LLM-搜索融合在最重要的主案例中没有真正体现**——评委最仔细看的就是主案例，而主案例的搜索是"上线前运行"的纯贝叶斯。
3. **运行轨迹缺失（2/3 验证主题）**——证据链底层断裂，评审无法追溯过程。
